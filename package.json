{
  "name": "pi-websearch-agora",
  "version": "1.0.0",
  "private": true,
  "description": "Pi agent extension porting Agora's WebSearchToolProvider (web_search + web_fetch) with Brave, Kagi, Serper, Tavily, SearXNG and DuckDuckGo backends.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "node --test --experimental-strip-types test/unit.test.ts",
    "test:registration": "node --experimental-strip-types test/registration.ts",
    "test:live": "node --experimental-strip-types test/execute.live.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
